-- =============================================================================
-- 0002_nopal_phases — project messages table for chat-style phase UI
-- =============================================================================

DEFINE TABLE IF NOT EXISTS project_messages TYPE ANY SCHEMALESS PERMISSIONS NONE;
