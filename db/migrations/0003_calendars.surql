-- =============================================================================
-- 0003_calendars — calendar collections and calendars tables
-- =============================================================================

DEFINE TABLE IF NOT EXISTS calendar_collections TYPE ANY SCHEMALESS PERMISSIONS NONE;
DEFINE TABLE IF NOT EXISTS calendars TYPE ANY SCHEMALESS PERMISSIONS NONE;
